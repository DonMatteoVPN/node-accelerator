# OS
.DS_Store
Thumbs.db

# editors
*.swp
*~
.vscode/
.idea/

# runtime / backups / logs (на случай если кто-то запустит локально)
*.bak
*.log
*.tmp
*.orig

# секреты не хранятся в этом репо (всё через ENV: CF_TOKEN, SECRET_KEY, WHITELIST),
# но на всякий случай — не коммитить ничего похожего:
*.key
*.pem
*_token*
*.env
